# ts-gn-log の作業の規則

## 文書とテストの題に書く断定

このリポジトリの PR 6 (fingerprint) のレビューで、回 2 から回 6 までの 5 回連続で「主張と実態の食い違い」が指摘された。原因は、断定を、それを検査する仕組みが無い置き場に書いたことにある。検査する仕組みが付いた対象 (ts のコードの挙動、ゴールデンベクタで照合する両言語の一致) は指摘が止まり、仕組みが無い対象 (py-gn-log の挙動、テストの題) は回をまたいで指摘が出続けた。経緯の記録は `docs/review-triages/` に置いてあるが、この置き場は `.gitignore` 済みなので、クローンには含まれない。

以下の 3 つを守る。**対象は、コードの挙動・両言語の一致・外部の実装の挙動についての断定** (docstring・README・テストの題に書くもの)。設計の意図、経緯の説明、参照先の案内はこの規則の対象ではない。

### 1. 断定は、リポジトリの関門で検査できるものに限る

関門とは、`npm run lint` / `npm run typecheck` / `npm test` / `npm run build` / `npm run check:dist` で失敗しうる仕組みを指す。テスト、ゴールデンベクタ、型がこれに当たる。

- 検査できない結末は書かない。書きたいなら先に関門を足す。
- まだ実装していない機能の挙動は、現在形で書かない。「PR N で実装する」と書く。

### 2. py-gn-log の挙動そのものは断定しない

ts のリポジトリから py-gn-log を実行する関門は無い。条件 (出力の形式、設定、出力先、ログのレベル) で結末が変わるものを一般化すると、確かめていない組み合わせが必ず残る。

- 書いてよいのは、正本の場所への参照 (「正本は py-gn-log の README の〜」) と、こちらで実行して確かめた 1 点の事実。
- 「py 側は〜になる」「残るのは〜だけ」のような、条件で変わる結末の一般化は書かない。条件の一覧と扱いの決定は py-gn-log の Issue に委ね、その番号を書く。
- 両言語の値の一致を断定するなら、`test/fixtures/fingerprint-golden.json` に事例を足して関門にする。
- Python 標準ライブラリへの言及 (`logging` のレベルの数値、`ContextVar` の位置づけ) はこの規則の対象外。py-gn-log の挙動の断定ではなく位置づけの説明で、断定の中に値が書かれているので読者が照合できる。

### 3. テストの題は、列挙した事例が属する分類だけを名指す

題が名指す分類が事例より広いと、分類に当てはまらない値が残る。テストの題については回 4・5・6 で 3 回続けて指摘が出た (`undefined` が既定値に吸われる、`"5"` が数値に変換できる、`null` が 0 に変換できる)。この規則を入れたときの棚卸しでは、配列がオブジェクトであることを見落とした題が 3 件と、全称の主張を 2 レベルしか通していない題が 1 件見つかった。

- 分類を名指すなら、その分類の境界を実行で確かめる。確かめないなら、題を事例に合わせて狭める。
- 「〜を問わない」のような全称の主張を書くなら、その軸の値を事例で網羅する。
- 実装が使っている判定の名前で書く (例: `isPlainObject` を使っているなら「オブジェクト以外」ではなく「プレーンなオブジェクト以外」)。

## コミット

コミットは、変更したファイルの種類ではなく**変更した動機**でグルーピングし、1 つのコミットに複数の動機を混ぜない。動機の例は、内容の追加・内容の修正・linter の指摘への対応・リファクタリング・ツールによる自動生成。`dist/` の再生成はツールによる自動生成なので、常に別のコミットにする。
