# ts-gn-log の作業の規則

## 文書とテストの題に書く断定

このリポジトリの PR 6 (fingerprint) のレビューで、回 2 から回 6 までの 5 回連続で「主張と実態の食い違い」が指摘された (記録の各回の指摘の要旨で数える。`category` では回 2 の該当指摘が `production` に入っているので 4 回に見える)。原因は、断定を、それを検査する仕組みが無い置き場に書いたことにある。検査する仕組みが付いた対象 (ts のコードの挙動、ゴールデンベクタで照合する両言語の一致) は指摘が止まり、仕組みが無い対象 (py-gn-log の挙動、テストの題) は回をまたいで指摘が出続けた。経緯の記録は `docs/review-triages/` に置いてあるが、この置き場は `.gitignore` 済みなので、クローンには含まれない。

以下の 3 つを守る。**対象は、コードの挙動・両言語の一致・外部の実装の挙動についての断定** (docstring・README・テストの題に書くもの)。設計の意図、経緯の説明、参照先の案内はこの規則の対象ではない。

### 1. 断定は、リポジトリの関門で検査できるものに限る

関門とは、`npm run lint` / `npm run typecheck` / `npm test` / `npm run build` / `npm run check:dist` と、CI だけが回す `npx --yes publint@0.3.24` (`.github/workflows/ci.yml`) で失敗しうる仕組みを指す。テスト、ゴールデンベクタ、型がこれに当たる。

- 検査できない結末は書かない。書きたいなら先に関門を足す。
- まだ実装していない機能の挙動は、現在形で書かない。「PR N で実装する」と書く。

### 2. py-gn-log の挙動そのものは断定しない

ts のリポジトリから py-gn-log を実行する関門は無い。条件 (出力の形式、設定、出力先、ログのレベル) で結末が変わるものを一般化すると、確かめていない組み合わせが必ず残る。

- 書いてよいのは、正本の場所への参照 (「正本は py-gn-log の README の〜」) と、こちらで実行して確かめた 1 点の事実。
- 「py 側は〜になる」「残るのは〜だけ」のような、条件で変わる結末の一般化は書かない。条件の一覧と扱いの決定は py-gn-log の Issue に委ね、その番号を書く。
- 両言語の値の一致を断定するなら、`test/fixtures/fingerprint-golden.json` に事例を足して関門にする。
- Python 標準ライブラリへの言及 (`logging` のレベルの数値、`ContextVar` の位置づけ) は、**位置づけの説明にとどめる限り**この規則の対象外。こちらの設計の根拠として使うなら対象に入る — 根拠にするなら、断定の中に値を書くか (`logging` のレベルの数値はこれに当たる)、こちら側の関門で検査できる形に言い換える。

### 3. 引数の値の分割は、docstring 1 箇所で語り、テストの題は値の表から生成する

「どの値を受け付け、どの値を弾くか」(値の分割) を、実装 1 箇所と散文 3〜5 箇所 (docstring・コメント・README・テストの題・この規則の例) で別々に書いていた。反例が指摘されるたびに散文の 1 箇所をその反例だけ除く形で書き換え、`maxLength` の題は 4 回、`fields` の記述は題 → コメント → docstring → 規則の例と 1 回に 1 箇所ずつ指摘された。値の空間を列挙してから書いたことが無かった。

- **分割を散文で語る場所は、判定の実装の隣の docstring 1 箇所だけ。** そこでは分類の語 (「オブジェクト以外」「プリミティブ」「数値に変換できる」) を使わず、判定の式を引用する (`typeof s !== "string"` なら既定、など)。コード内のコメント・README・この規則は、判定関数の名前を参照するだけで分割を言い換えない。
- **テストの事例は `typeof` の分割を尽くす表にし、題は表から生成する。** `typeof` の 8 種で値の空間を尽くし、object はさらに null・配列・それ以外 (class のインスタンスで代表) に分ける。`test/helpers.ts` の `TYPEOF_SPACE` がその表で、表の作りと使い方の正本はその docstring。object の下位 (`Date` や `Map` など) を分ける必要が出たら表に行を足す。テストは `typeofRows({...})` に値ごとの期待を並べ、`it.each` が題を組み立てる。期待の表は `Record<TypeofName, E>` なので、値を足すと期待を書いていないテストが typecheck で落ちる。
- **「〜を問わない」「すべて」のような全称の主張は、その軸の定数 (`LEVELS` など) を反復する。** リテラルを書き並べない。

### 4. 根拠のある主張を書き写さない。正本を 1 つ定め、他はリンクにする

レビューの 9 回目までに、主張そのものは真なのに書き写しで限定 (条件・時制・主語・環境・検査の有無) が落ちる形が 7 件あった。レビュアの実験の結果をテストの題に書く、他の文書の条件つきの文を条件を落として写す、実測の環境を落とす、計画を現在形で書く、他リポジトリへの提案を契約と書く、など。根拠の在り処がこのリポジトリの関門であるものは 1 つも無かった。

- 主張ごとに正本を 1 つ定める (決定なら設計案の該当項、挙動なら判定の隣の docstring、テストの題ならテスト本体)。正本には「この項が正本」と書く。
- 正本以外の場所には主張を書き写さず、正本へのリンクだけを置く。要約も置かない。値・条件・時制を伴う主張は特に。
- テストの題は、テスト本体が検査することだけを述べる。レビュアの実験や別のテストで確かめた結末を題に足さない。足したいなら本体に検査を足す。
- 参照が循環していないことを `doc-dag` で確かめる (向き: 参照元 → 正本)。

## コミット

コミットは、変更したファイルの種類ではなく**変更した動機**でグルーピングし、1 つのコミットに複数の動機を混ぜない。動機の例は、内容の追加・内容の修正・linter の指摘への対応・リファクタリング・ツールによる自動生成。`dist/` の再生成はツールによる自動生成なので、常に別のコミットにする。
